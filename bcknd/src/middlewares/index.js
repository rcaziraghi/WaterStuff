const authJwt = require("./authJwt");
const verificarRegistrar = require("./verificarRegistrar");
const verificarRecSenha = require("./verificarRecSenha");

module.exports = {
  authJwt,
  verificarRegistrar,
  verificarRecSenha
};
